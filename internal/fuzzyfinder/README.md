# Local fuzzy finder adaptation

This package is based on `github.com/ktr0731/go-fuzzyfinder` v0.9.0. It keeps
the upstream terminal UI while adding a custom action hotkey that returns the
highlighted item. The matching engine remains imported from the upstream Go
module. See `LICENSE` for the upstream license.
